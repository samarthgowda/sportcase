const Button = {
  baseStyle: {
    borderRadius: "full",
  },
  sizes: {
    xs: {
      borderRadius: "full",
    },
    sm: {
      borderRadius: "full",
    },
    md: {
      borderRadius: "full",
    },
    lg: {
      borderRadius: "full",
    },
  },
};

export default Button;
